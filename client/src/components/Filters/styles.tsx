import { css } from "@emotion/css";

export const styles = css`
  background-color: white;
  padding: 24px;
  position: relative;
  display: flex;

  .filters {
    max-width: calc(100% - 200px);
  }
`;
